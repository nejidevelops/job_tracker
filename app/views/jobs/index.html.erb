<h1>All Jobs</h1>

<% @jobs.each do |job| %>
    <h2><%= link_to job.title, job_path(job) %></h2>
    <p><%= job.company %></p>
    <p><%= job.status %></p>
    <p><%= job.notes %></p>
<% end %>

<%= link_to "Add New Job", new_job_path %>